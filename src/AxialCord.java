public class AxialCord {

    private int[] x = new int[]{1, 2, 3, 4, 5, 6, 7, 8, 9};
    private char[] y = new char[]{'a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i'};

    public AxialCord(int x, char y) {
        this.x[x] = x;
        this.y[y] = y;
    }

    public int axialToIndex() {
        //TODO * Returns axial coordinate into number index (user readable)

        return 0;
    }
}
